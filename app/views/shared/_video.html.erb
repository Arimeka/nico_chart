  <td rowspan="2">
    <a class="thumbnail" data-toggle="modal" href="<%= "#playVideo" + i.to_s %>" id="<%= "thumbnail" + i.to_s %>">
      <%= render :partial => 'shared/video/thumbnail', :locals => {:x => x} %>
    </a>
  </td>
  <td>
    <strong>+<%= value_gain(x, controller.action_name) %></strong>
  </td>
  <td>
    <%= link_to '<i class="icon-repeat"></i>'.html_safe, 
        { :controller => controller.controller_name , :action => "refind", 
          :id => x.nico_id, :remote => true, 
          :contr => controller.controller_name, :number => i }, 
        :class => "btn btn-danger pull-right", :title => "Re-find video"
     %>
    <strong><%= x.upload_date %></strong>
    <span class="label" id="<%= "videoSource" + i.to_s %>">
      <% if youtube?(x) %>
        YouTube
      <% else %>
        Nicovideo
      <% end %>
    </span>    
  </td>
</tr>
<tr>
  <td colspan="2">
    <% if youtube?(x) %>
      <a href="<%= "http://www.youtube.com/watch?v=" + x.youtube_id %>" target="_blank" 
        id="<%= "youtube_url" + i.to_s %>">            
    <% else %>
      <a href="<%= "http://www.nicovideo.jp/watch/" + x.nico_id %>" target="_blank">          
    <% end %>         
      <h2><%= x.title %></h2>
    </a>
    <div class="modal hide" id="<%= "playVideo" + i.to_s %>">
      <%= render :partial => 'shared/video/play_video', :locals => {:x => x, :i => i} %>
    </div>
    <div class="btn" data-toggle="modal" data-target="<%= "#playVideo" + i.to_s %>">
      <i class="icon-play"></i> Play
    </div>
    <a class="btn" href="<%= "http://nicosound.anyap.info/sound/" + x.nico_id %>" target="_blank">
      Find MP3 <i class="icon-music"></i>
    </a>
  </td>
</tr>